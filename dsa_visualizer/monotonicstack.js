export function monotonicStack(arr){
    if(arr.length<=1)
    return;

    const animateArr = [];
    monotonicStackUtility(arr,animateArr);
    return animateArr;
}

function monotonicStackUtility(arr,animateArr){



}
